{{- include "ffc-helm-library.deployment" (list . "ffc-pay-statement-data.deployment") -}}
{{- define "ffc-pay-statement-data.deployment" -}}
spec:
  template:
    spec:
      containers:
      - {{ include "ffc-helm-library.container" (list . "ffc-pay-statement-data.container") }}
{{- end -}}
